// unit: app (app)
// capabilities: run
// effects: store, net
// uses: users, tasks, notifier
// GENERATED SKELETON — edges are declared in the project spec.

import * as users from "../../users/src/index.js";
import * as tasks from "../../tasks/src/index.js";
import * as notifier from "../../notifier/src/index.js";

export function run(): void {
  // TODO
}

